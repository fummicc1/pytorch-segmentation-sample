version: '3'
services:
  ink-detection:
    build: .
    shm_size: 6gb
    volumes:
      - ./:/workspace
    tty: true
    deploy:
      resources:
        reservations:
          devices:
            - driver: "nvidia"
              count: all
              capabilities: [ gpu ]
